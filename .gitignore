build/
managed_components/
sdkconfig
sdkconfig.old
dependencies.lock
.vscode/
.idea/
*.bin
*.elf
*.map
*.log
.DS_Store

# Private dev notes / PII
# CLAUDE.md holds internal infra (host IPs, MACs, order numbers, paths).
# Other *.local.md are a convention for machine-specific scratch notes.
CLAUDE.md
CLAUDE.local.md
*.local.md
NOTES.md
.claude/

# Secrets / credentials — defensive, not currently used
.env
.env.*
*.pem
*.key
*.p12
*.pfx
secrets/
secrets.yaml
credentials.json

# Captured sensor data / debug traces — may contain location or context
*.csv
captures/

